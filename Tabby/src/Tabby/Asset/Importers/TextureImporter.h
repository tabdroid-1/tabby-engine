// #pragma once
//
// #include <Tabby/Renderer/Texture.h>
//
// namespace Tabby {
//
// class TextureImporter {
// public:
//     // AssetMetadata filepath is relative to project asset directory
//     // static Ref<Texture> ImportTexture2D(AssetHandle handle, const AssetMetadata& metadata);
//
//     // Reads file directly from filesystem
//     // (i.e. path has to be relative / absolute to working directory)
//     static Shared<Texture> LoadTexture(const std::filesystem::path& path);
// };
//
// }
